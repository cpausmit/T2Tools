[cfg]
user = paus
server = submit50.mit.edu
prefix = 
base = /data/submit

[commands]
ls = ls -l -d $src/*
rm = rm $src
rmr = rm -r $src
mv = mv $src
cp = cp $src $tgt
rmdir = rmdir $src
mkdir = mkdir $src
test = test -e $src
testDir = test -d $src
du = du -h $src
